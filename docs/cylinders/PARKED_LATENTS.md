# Latents (parked)

VAE/CLIP latent grids are strong for **index/retrieve**, weak as default **wire format** to commercial vision APIs (which expect images). Parked: no generic Latents cylinder; future optional `latent_index` only.

**Status:** research only · not default send path
