# split-exec / envelope

Binds job + document into a cheaper loyal send layout; may choose among candidate envelopes. v0.4.3+: `pick_gated_envelope` tries ≥2 ranked candidates against the tokenizer gate.

**Code:** `packages/engine/tokenish_engine/compile/lcs.py` (`assemble_envelope`, `pick_gated_envelope`)
