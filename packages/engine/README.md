# Tokenish Engine

FastAPI Split-Execution optimizer.

```bash
pip install -e ".[dev]"
uvicorn tokenish_engine.app:app --port 8741
```

Endpoints: `GET /health`, `GET /providers`, `POST /compile`, `POST /chat`.
